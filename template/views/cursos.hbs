{{>header}}
<div class="container">
  {{#if aspirante}}
    {{{listarCursosInteresado cursos}}}
  {{/if}}
  {{#if coordinador}}
    {{{listarCursosCoordinador cursos cursoXUsuario}}}
  {{/if}}
  {{#if docente}}
    {{{listarCursosDocente cursos cursoXUsuario}}}
  {{/if}}
</div>

{{>footer}}
